import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'basic-project',
  webDir: 'dist/basic-project',
  bundledWebRuntime: false
};

export default config;
